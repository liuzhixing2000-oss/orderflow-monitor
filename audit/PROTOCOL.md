# Frozen audit protocol — 2026-09-07

This is an audit of an already explored sample, not threshold optimization or an out-of-sample test. No live files or parameters are changed. Primary previously selected hypothesis: ETH continuation-long crossing 70, 240-minute return, 240-minute cooldown, 0.12% round-trip cost. Report all original symbols, sides, thresholds and horizons to preserve the search history.

## Data and provenance

Research source: edc3ce0ab951d874f8740469ebc45e724d9ea08a. Legacy source: b2045f3575281ad8ef6670b82dac4bd1daaaf81d. Vendor the two original replay scripts without editing them. Exact per-symbol Sample A cutoff comes from the first replay's LEGACY_REPLAY_META, not from a rounded wall-clock date. Deduplicate logs by (symbol, payload timestamp); fail on conflicting payloads. Save canonical JSONL, deterministic gzip, SHA256 and per-symbol bounds/counts. Recovered logs are a subset, never relabelled a complete SQLite export. Compare rerun aggregates against the first replay, not the later growing-database replay.

## Fixed diagnostics

1. Enumerate every crossing and every horizon, including missing exits. Reproduce the original replay exactly on the recovered subset before interpreting diagnostics. Include entry/exit timestamps, scores, features and source-payload hashes. Summarize one-minute feature availability and legacy/live formula parity on identical inputs with book excluded.
2. Live 10-second data cannot be reconstructed from minute snapshots. Do not interpolate missing order flow or claim sampling parity. Measure any available exact formula mismatches separately from unavailable input parity. Compare recent stored 10-second price statistics with deterministic coarsening only if original fine data is available.
3. Price-only comparison: positive/negative 1h and 4h returns meeting the existing 0.05%/0.15% minimum moves, without Delta or OI in the gate. Evaluate first entries with the same 240-minute cooldown. This diagnostic is a distinct benchmark, not a retuned score.
4. Matched controls: same symbol and direction, quality-eligible, price-only trend gate, drawn from the preceding 48 hours, with the control's 240-minute outcome completed before the signal time. Match on 5m/15m/1h/4h returns, 1h/4h directional efficiency, and 1h realized volatility. Scale using only earlier price-eligible rows with median/MAD (standard-deviation fallback). Require >=30 scaling rows; per-coordinate standardized distance <=2 and RMS distance <=1. Select up to five nearest controls, at least 240 minutes apart within a set. No exclusion based on their returns, score or later outcome. Report unmatched signals, control reuse and effective dates; do not widen calipers if coverage is poor. Controls may themselves be signal observations, so this is an unconditional price-conditioned comparison, not a low-score contrast.
5. Unscaled component ablations: jointly mask Delta-derived terms, OI, or both using None, in BOTH regime and continuation functions; keep thresholds and all remaining weights unchanged. Report score and eligibility changes at original event times plus full crossing counts/returns for each ablation. A disappearance of high-score events is score mechanics, not proof of informational edge; no threshold rescaling to restore counts.
6. Report leave-one-event-out, leave-one-UTC-day-out, and leave-one-symbol-out results. Four-hour cooldown is per symbol/side/threshold; overlap across symbols and thresholds remains. Event observations are not automatically statistically independent.
7. Report realized holding times and missing/path-gap counts. Legacy endpoint-return figures retain original exit tolerance. Add separate coverage-qualified summaries rather than silently changing the historical results. Costs are fixed proxy costs, excluding separately observed funding and executable quote slippage.
8. BTC beta attribution supplement (specified before its calculation): for ETH/SOL original events, estimate beta with an intercept from non-overlapping 15-minute returns in the preceding 24 hours, at least 32 paired returns. Use only prices at or before grid endpoints with <=90 seconds staleness, and endpoints strictly before the signal. Report BTC holding-window return, beta, and signal net return minus beta times directional BTC gross return. Retain original signal cost; this residual is a statistical decomposition, NOT an executable hedged-strategy return (no hedge costs/funding). Do not choose a lookback by performance. It removes only this estimated BTC market exposure, not all trend or volatility effects.

## Inference restrictions

No significance claim or optimization from A. No confidence interval from minute-level IID resampling. Fewer than ten independent UTC days is inadequate here for a reliable day-cluster interval; report descriptive matched differences and sensitivity only. A positive matched difference is not causal proof. ETH/70/240 was selected after examining A; every analysis in this audit remains exploratory.

## Forward confirmation specification (not an installed automation)

Sample B must begin only after the complete protocol and implementation are committed, with one frozen code/data version, fresh completed warmup and no reuse of Sample A outcomes. Hold ETH/70/240 as the sole primary previously selected hypothesis; other combinations are secondary. Record all qualifying opportunities and exclusions, not just events. Collect at least 30 UTC days and 100 primary events, whichever is later, before the first confirmatory decision; these are evidence gates, not a guarantee of power. Use a synchronized pure-price comparator, matched-price incremental returns and UTC-day block inference. Require positive net return and positive incremental effect with uncertainty excluding zero, plus stability outside the best day. A failed/missing gate means inconclusive or rejected, not permission to retune B. A new scoring version requires a new prospective sample.
